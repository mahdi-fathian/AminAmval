import {icon,esc,fa,date,time,isoDate,today,size,roleNames,statusNames,qualityNames,actionNames,entityNames,badge,initials,button,empty,field,select} from './ui.js';
import {isAdmin,isStaff,passwordFields} from './views.js';
const cancel=()=>button('انصراف','close-modal','','');
const submit=(label,id,cls='primary',ico='check')=>`<button type="submit" form="${id}" class="btn ${cls}">${icon(ico)}${esc(label)}</button>`;
const error='<div class="form-error" role="alert"></div>';
const requiredNote='<span class="footer-note">فیلدهای ستاره‌دار الزامی هستند.</span>';
const datesHint='تاریخ ورودی میلادی است؛ در سامانه به صورت شمسی نمایش داده می‌شود.';
function miniAsset(a){return `<div class="mini-asset"><img class="asset-image" src="/api/files/${esc(a.imageId)}" alt=""><div><strong>${esc(a.name)}</strong><p class="ltr">${esc(a.code)}</p></div>${badge(a.status)}</div>`}
export function assetForm(S,a=null){const editing=!!a;return {title:editing?'ویرایش شناسنامهٔ اموال':'ثبت اموال جدید',subtitle:'یک شناسنامهٔ دقیق برای دارایی سازمان',operation:editing?'asset.edit':'asset.create',entityId:a?.id,size:'large',body:`<form id="asset-form" data-form="asset" data-id="${a?.id||''}" data-version="${a?.version||0}">${error}<div class="form-grid"><div class="form-section">${icon('box')}مشخصات اصلی اموال</div>${field('نام اموال','name',a?.name,{required:true,max:200,placeholder:'مثلاً لپ‌تاپ واحد فناوری اطلاعات'})}${field('کد اموال','code',a?.code,{max:60,ltr:true,placeholder:'تولید خودکار در صورت خالی بودن',hint:'کد باید یکتا باشد؛ کدهای بایگانی‌شده قابل تکرار نیستند.'})}${field('کد اموال قبلی','oldCode',a?.oldCode,{max:60,ltr:true,placeholder:'در صورت وجود'})}${select('دسته‌بندی','categoryId',S.lookups.categories,a?.categoryId,{required:true,quick:'categories'})}${field('برند','brand',a?.brand,{max:100,placeholder:'مثلاً Lenovo',hint:'حداقل یکی از دو فیلد برند یا مدل الزامی است.'})}${field('مدل','model',a?.model,{max:100,placeholder:'مثلاً ThinkPad T14'})}${field('شماره سریال','serial',a?.serial,{max:100,ltr:true,placeholder:'در صورت وجود'})}${select('کیفیت اموال','quality',Object.entries(qualityNames).map(([id,name])=>({id,name})),a?.quality||'Good',{required:true})}${field('مالک اموال','owner',a?.owner,{required:true,max:150,placeholder:'مثلاً ناواکو یا بانک',attrs:'list="owner-options"'})}<datalist id="owner-options">${[...new Set(['ناواکو','بانک',...S.lookups.owners])].map(x=>'<option value="'+esc(x)+'">').join('')}</datalist>${field('محل نگهداری','location',a?.location,{max:200,placeholder:'مثلاً انبار مرکزی، طبقهٔ دوم'})}<div class="form-section">${icon('file')}اطلاعات خرید و شناسایی</div>${field('تاریخ خرید (میلادی)','purchaseDate',isoDate(a?.purchaseDate),{type:'date',ltr:true,maxDate:today(),hint:datesHint})}${field('ارزش خرید (ریال)','purchaseCost',a?.purchaseCost??'',{type:'number',ltr:true,min:'0',attrs:'max="1000000000000000" step="1"',placeholder:'اختیاری، به ریال'})}<div class="check-card span-full"><label class="check-label"><input type="checkbox" name="hasLabel" ${a?.hasLabel!==false?'checked':''}>اموال دارای برچسب فیزیکی است</label><small>با برداشتن تیک، این مال در گزارش «اموال بدون برچسب» نمایش داده می‌شود.</small></div><div class="form-field full"><label>تصویر اموال <span class="required">*</span></label><div class="upload-box"><div class="upload-preview" id="asset-image-preview">${a?.imageId?`<img src="/api/files/${esc(a.imageId)}" alt="پیش‌نمایش تصویر اموال">`:icon('image')}</div><div class="upload-copy"><h3>تصویر واضحی از اموال بارگذاری کنید</h3><p>PNG، JPG یا WebP · حداکثر ۵ مگابایت</p><label class="btn small" for="asset-image-file">${icon('upload')}انتخاب تصویر</label><input type="file" class="sr-only" id="asset-image-file" accept="image/png,image/jpeg,image/webp"><input type="hidden" name="imageId" value="${esc(a?.imageId||'')}"><span id="image-upload-status" class="secondary-text" aria-live="polite"></span></div></div></div>${field('توضیحات','description',a?.description,{required:true,textarea:true,full:true,max:2000,placeholder:'مشخصات تکمیلی، وضعیت ظاهری و توضیحات مورد نیاز…'})}</div></form>`,footer:requiredNote+cancel()+submit(editing?'ذخیرهٔ تغییرات':'ثبت اموال','asset-form')};}
export function assignmentForm(a,transfer=false){return {title:transfer?'انتقال اموال':'تخصیص و تحویل اموال',subtitle:transfer?'بستن تخصیص قبلی و ثبت تحویل به گیرندهٔ جدید':'تخصیص یک مال به شخص یا واحد سازمانی',operation:transfer?'transfer':'assign',entityId:a.id,body:'',footer:'',build(S){this.body=`${miniAsset(a)}<form id="assignment-form" data-form="assignment" data-id="${a.id}" data-version="${a.version}" data-transfer="${transfer}">${error}<div class="form-grid"><div class="segmented span-full"><label><input type="radio" name="targetType" value="user" checked>تخصیص به شخص</label><label><input type="radio" name="targetType" value="department">تخصیص به واحد</label></div><div class="span-full" id="assign-user-field">${select('تحویل‌گیرنده','userId',S.lookups.users.filter(x=>x.departmentId&&x.role==='Employee').map(x=>({id:x.id,name:x.name+' · '+x.personnelCode})),null,{required:true,placeholder:'انتخاب کارمند فعال',hint:'اموال فقط به کارکنان سازمان (نقش «کاربر عادی») قابل تخصیص است.'})}</div><div class="span-full hidden" id="assign-department-field">${select('واحد سازمانی','departmentId',S.lookups.departments,null,{disabled:true})}</div>${field('تاریخ شروع / انتقال (میلادی)','startedAt',today(),{required:true,type:'date',ltr:true,maxDate:today(),hint:datesHint})}${field('شمارهٔ رسید تحویل','reference','',{max:200,placeholder:'اختیاری'})}${field('توضیحات تحویل','notes','',{textarea:true,full:true,max:1000,placeholder:transfer?'علت جابه‌جایی و توضیحات تحویل…':'وضعیت اموال هنگام تحویل و توضیحات تکمیلی…'})}</div></form>`;this.footer=cancel()+submit(transfer?'ثبت انتقال':'ثبت تخصیص','assignment-form','primary','assign');return this}}}
export function returnForm(a){return {title:'عودت اموال به انبار',subtitle:'تخصیص جاری بسته و اموال برای تحویل بعدی آماده می‌شود.',operation:'return',entityId:a.id,body:`${miniAsset(a)}<form id="return-form" data-form="return" data-id="${a.id}" data-version="${a.version}">${error}<div class="form-grid">${field('تاریخ عودت (میلادی)','date',today(),{required:true,type:'date',ltr:true,maxDate:today(),hint:datesHint})}${field('شماره رسید عودت','reference','',{max:200})}${field('محل نگهداری جدید','location',a.location,{max:200,full:true,placeholder:'مثلاً انبار مرکزی'})}${field('علت و شرح عودت','reason','',{required:true,textarea:true,full:true,max:1000,placeholder:'وضعیت اموال هنگام عودت و علت بازگشت…'})}</div></form>`,footer:cancel()+submit('ثبت عودت','return-form','primary','return')};}
export function statusForm(a){const values=['Maintenance','Lost','Scrapped','Sold','Exited',...(['Maintenance','Lost'].includes(a.status)?['Available']:[])].filter(x=>x!==a.status);return {title:'تغییر وضعیت و خروج از چرخه',subtitle:'ثبت مجوز اداری برای ردیابی و حسابرسی ضروری است.',operation:'status',entityId:a.id,body:`${miniAsset(a)}<div class="alert warning">${icon('warning')}فروش، اسقاط و خروج ابتدا به‌صورت درخواست ثبت می‌شوند. تأیید مدیرِ غیر از ثبت‌کننده الزامی است. تا تصمیم مدیر، مال قفل می‌شود؛ پس از تأیید، وضعیت نهایی برگشت‌پذیر نیست.</div><form id="status-form" data-form="status" data-id="${a.id}" data-version="${a.version}">${error}<div class="form-grid">${select('وضعیت جدید','status',values.map(id=>({id,name:statusNames[id]})),null,{required:true})}${field('تاریخ عملیات (میلادی)','date',today(),{required:true,type:'date',ltr:true,maxDate:today()})}${field('شمارهٔ مجوز / صورت‌جلسه','reference','',{required:true,max:200,full:true,placeholder:'شناسهٔ سند تأییدکنندهٔ عملیات'})}<div id="sale-amount-field" class="span-full hidden">${field('مبلغ فروش (ریال)','amount','',{type:'number',min:'1',ltr:true,attrs:'max="1000000000000000" step="1" disabled'})}</div>${field('شرح و علت عملیات','reason','',{required:true,textarea:true,full:true,max:1000})}<div class="check-card span-full"><label class="check-label"><input type="checkbox" name="approved" required>اطلاعات و مستند اداری این عملیات را بررسی کرده‌ام.</label></div></div></form>`,footer:cancel()+submit('ثبت تغییر وضعیت','status-form','primary','check')};}
export function deleteAsset(a){return {title:'بایگانی اموال',subtitle:'این عملیات فقط برای مدیر سامانه مجاز است.',operation:'asset.delete',entityId:a.id,size:'small',body:`<div class="alert danger">${icon('warning')}«${esc(a.name)}» از فهرست فعال خارج می‌شود. تاریخچه و کد اموال برای حسابرسی محفوظ خواهد ماند.</div><form id="delete-asset-form" data-form="delete-asset" data-id="${a.id}" data-version="${a.version}">${error}${field('علت بایگانی','reason','',{required:true,textarea:true,max:1000,placeholder:'علت دقیق این عملیات را ثبت کنید…'})}</form>`,footer:cancel()+submit('بایگانی اموال','delete-asset-form','danger','trash')};}
function detailItem(label,value,full=false){return `<div class="detail-item ${full?'full':''}"><dt>${esc(label)}</dt><dd>${esc(value??'—')}</dd></div>`}
export function assetDetail(S,d,tab='info'){const a=d.asset,staff=isStaff(S);const terminal=['Scrapped','Sold','Exited'].includes(a.status);return {title:'شناسنامهٔ اموال',subtitle:staff?'مشخصات، تخصیص‌ها و تاریخچهٔ تغییرات':'مشخصات اموال تحویلی شما',size:'drawer',entityId:a.id,detailType:'asset',body:`<div class="detail-hero"><img class="detail-image" src="/api/files/${esc(a.imageId)}" alt="${esc(a.name)}"><div>${badge(a.status)}<h2>${esc(a.name)}</h2><p>${esc(a.categoryName)} · ${esc([a.brand,a.model].filter(Boolean).join(' / '))}</p><p class="ltr mono" style="margin-top:7px;color:var(--blue)">${esc(a.code)}</p></div></div>${staff?assetActionButtons(S,a):''}<div class="detail-tabs"><button data-action="detail-tab" data-tab="info" data-id="${a.id}" class="${tab==='info'?'active':''}">مشخصات اموال</button>${staff?`<button data-action="detail-tab" data-tab="assignments" data-id="${a.id}" class="${tab==='assignments'?'active':''}">تاریخچهٔ تخصیص <span class="muted">(${fa(d.assignments.length)})</span></button><button data-action="detail-tab" data-tab="history" data-id="${a.id}" class="${tab==='history'?'active':''}">سوابق عملیات</button>`:''}</div>${tab==='info'?`<dl class="detail-grid">${detailItem('کد اموال',a.code)}${detailItem('کد قبلی',a.oldCode||'ثبت نشده')}${detailItem('شماره سریال',a.serial||'ثبت نشده')}${detailItem('دسته‌بندی',a.categoryName)}${detailItem('برند / مدل',[a.brand,a.model].filter(Boolean).join(' / '))}${detailItem('کیفیت',qualityNames[a.quality])}${detailItem('مالک اموال',a.owner)}${detailItem('وضعیت برچسب',a.hasLabel?'دارای برچسب':'بدون برچسب')}${detailItem('محل نگهداری',a.location||'—')}${detailItem('تاریخ خرید',date(a.purchaseDate))}${staff?detailItem('ارزش خرید',a.purchaseCost==null?'ثبت نشده':fa(a.purchaseCost)+' ریال'):''}${detailItem('تاریخ ثبت',date(a.createdAt))}${detailItem('تحویل‌گیرندهٔ جاری',a.currentAssignment?.userName||(a.currentAssignment?'تخصیص به واحد':'تخصیص داده نشده'))}${detailItem('واحد بهره‌بردار',a.currentAssignment?.departmentName||'—')}${detailItem('شروع تخصیص',date(a.currentAssignment?.startedAt))}<div class="detail-item full"><dt>توضیحات</dt><dd class="detail-note">${esc(a.description)}</dd></div></dl>`:tab==='assignments'?d.assignments.length?`<div class="table-scroll"><table class="data-table"><thead><tr><th>تحویل‌گیرنده</th><th>از تاریخ</th><th>تا تاریخ</th><th>رسید و توضیحات</th></tr></thead><tbody>${d.assignments.map(x=>`<tr><td>${esc(x.userName||'تخصیص به واحد')}<span class="secondary-text">${esc(x.departmentName)}</span></td><td>${date(x.startedAt)}</td><td>${x.endedAt?date(x.endedAt):'<span class="badge green">جاری</span>'}<span class="secondary-text">${esc(x.endReason)}</span></td><td class="description-cell">${esc(x.reference||'—')}<span class="secondary-text">${esc(x.notes)}</span><div class="actions">${button('رسید تحویل','print-receipt','download','small','data-id="'+x.id+'"')}${x.endedAt?button('رسید خاتمه','print-receipt','download','small','data-id="'+x.id+'" data-kind="return"'):''}</div></td></tr>`).join('')}</tbody></table></div>`:empty('هنوز سابقهٔ تحویلی ثبت نشده','با تخصیص اموال، اطلاعات تحویل، انتقال و عودت در این بخش ثبت می‌شود.','assign','',true):`<div class="row-between" style="margin-bottom:20px"><span class="muted" style="font-size:10px">${fa(d.historyTotal)} رویداد ثبت‌شده · زمان نمایش: تهران</span>${button('خروجی کامل تاریخچه','download','excel','small','data-url="/api/assets/'+a.id+'/history/export"')}</div><div class="timeline-list">${d.history.map(x=>`<div class="timeline-entry"><h4>${esc(x.description)}</h4><div class="timeline-meta"><span>${esc(x.actorName)}</span><span>${date(x.at)} · ${time(x.at)}</span><span class="ltr">IP: ${esc(x.ip)}</span></div>${x.before||x.after?button('جزئیات تغییر','audit-detail','eye','small','data-id="'+x.id+'"'):''}</div>`).join('')}</div>${d.history.length<d.historyTotal?`<div class="text-center" style="margin-top:20px">${button('مشاهدهٔ سوابق بیشتر','more-history','history','','data-id="'+a.id+'"')}</div>`:''}`}`,footer:button('بستن','close-modal','','')};}
export function userForm(S,u=null){const editing=!!u;const system=editing&&!u.nationalId&&['admin','jamdar'].includes(u.username);return {title:editing?'ویرایش اطلاعات کاربر':'افزودن کاربر جدید',subtitle:'تعریف مشخصات پرسنلی و نقش سازمانی',operation:editing?'user.edit':'user.create',entityId:u?.id,body:`<form id="user-form" data-form="user" data-id="${u?.id||''}" data-version="${u?.version||0}">${error}<div class="form-grid">${field('نام','firstName',u?.firstName,{required:true,max:100})}${field('نام خانوادگی','lastName',u?.lastName,{required:true,max:100})}${field('کد پرسنلی','personnelCode',u?.personnelCode,{required:true,max:60,ltr:true,placeholder:'مثلاً 1001'})}${field('کد ملی','nationalId',u?.nationalId,{required:!system,max:10,ltr:true,attrs:'inputmode="numeric"',placeholder:system?'برای حساب سیستمی اختیاری است':'۱۰ رقم معتبر با حفظ صفر ابتدایی'})}${select('واحد سازمانی','departmentId',S.lookups.departments,u?.departmentId,{required:!system,quick:'departments'})}${field('نام کاربری','username',u?.username,{required:editing,max:60,ltr:true,autocomplete:'off',placeholder:'پیش‌فرض: کد پرسنلی',hint:'۳ تا ۶۰ نویسهٔ لاتین، عدد و . _ -'})}${isAdmin(S)?editing?select('نقش کاربری','role',Object.entries(roleNames).map(([id,name])=>({id,name})),u?.role,{required:true}):'<div class="form-field"><label>نقش کاربری</label><select name="role" required><option value="Employee" selected>کاربر عادی</option></select><small class="hint">مدیر سامانه و جمعدار اموال فقط از مسیر راه‌اندازی اولیه قابل ایجاد هستند.</small></div>':'<input type="hidden" name="role" value="Employee">'}${!editing?field('گذرواژهٔ اولیه (اختیاری)','password','',{type:'password',max:128,autocomplete:'new-password',hint:'خالی = کد ملی؛ رمز سفارشی باید حداقل ۱۲ نویسهٔ قوی باشد.'}):''}<div class="check-card span-full"><label class="check-label"><input type="checkbox" name="active" ${u?.active!==false?'checked':''}>حساب کاربری فعال باشد</label><small>پیش از غیرفعال‌سازی، تمام اموال تحویلی کاربر باید عودت یا انتقال داده شوند.</small></div></div>${!editing?'<div class="alert" style="margin-top:18px">'+icon('shield')+'تغییر گذرواژهٔ اولیه در اولین ورود الزامی است. نام کاربری و رمز اولیه را از مسیر امن به کاربر تحویل دهید.</div>':''}</form>`,footer:requiredNote+cancel()+submit(editing?'ذخیرهٔ تغییرات':'ثبت کاربر','user-form')};}
export function userDetail(S,d){const u=d.user;return {title:'پروندهٔ کاربر',subtitle:'اطلاعات پرسنلی و فهرست اموال تحویلی',size:'large',entityId:u.id,body:`<div class="account-header"><span class="avatar large">${esc(initials(u))}</span><div><h2>${esc(u.fullName)}</h2><p>${roleNames[u.role]} · ${esc(u.departmentName||'حساب سیستمی')}</p></div><span class="badge ${u.active?'green':'red'}">${u.active?'فعال':'غیرفعال'}</span></div><div class="detail-actions">${isAdmin(S)||u.role==='Employee'?button('ویرایش مشخصات','edit-user','edit','','data-id="'+u.id+'"')+button('بازنشانی گذرواژه','reset-password','lock','','data-id="'+u.id+'"'):''}${isAdmin(S)?button('سوابق کاربر','user-history','history','','data-id="'+u.id+'"'):''}</div><dl class="detail-grid">${detailItem('کد پرسنلی',u.personnelCode)}${detailItem('نام کاربری',u.username)}${detailItem('کد ملی',u.nationalId||'حساب سیستمی')}${detailItem('واحد سازمانی',u.departmentName||'—')}${detailItem('تاریخ ایجاد حساب',date(u.createdAt))}${detailItem('وضعیت گذرواژه',u.mustChangePassword?'نیازمند تغییر در ورود بعدی':'گذرواژهٔ شخصی')}</dl><div class="form-section" style="margin:25px 0 15px">${icon('box')}اموال تحویلی جاری <span class="badge blue">${fa(d.assets.length)} مورد</span></div>${d.assets.length?`<div class="table-scroll"><table class="data-table"><thead><tr><th>اموال</th><th>دسته‌بندی</th><th>تاریخ تحویل</th><th>مشاهده</th></tr></thead><tbody>${d.assets.map(a=>`<tr><td><span class="asset-name">${esc(a.name)}</span><span class="asset-code">${esc(a.code)}</span></td><td>${esc(a.categoryName)}</td><td>${date(a.currentAssignment?.startedAt)}</td><td><button class="icon-btn" data-action="asset-detail" data-id="${a.id}" aria-label="مشاهده اموال">${icon('eye')}</button></td></tr>`).join('')}</tbody></table></div>`:empty('اموالی به این کاربر تحویل داده نشده','تخصیص‌های جاری این همکار در این قسمت نمایش داده می‌شوند.','box','',true)}`,footer:button('بستن','close-modal')};}
export function referenceForm(kind,item=null){return {title:(item?'ویرایش ':'افزودن ')+(kind==='categories'?'دسته‌بندی اموال':'واحد سازمانی'),subtitle:'ساختار اطلاعات سازمان را یکپارچه نگه دارید.',size:'small',operation:'reference',entityId:item?.id,body:`<form id="reference-form" data-form="reference" data-kind="${kind}" data-id="${item?.id||''}" data-version="${item?.version||0}">${error}<div class="form-grid">${field(kind==='categories'?'عنوان دسته‌بندی':'نام واحد سازمانی','name',item?.name,{required:true,max:100,full:true,placeholder:kind==='categories'?'مثلاً تجهیزات رایانه‌ای':'مثلاً فناوری اطلاعات'})}${kind==='categories'?field('توضیحات','description',item?.description,{textarea:true,max:1000,full:true}):''}</div></form>`,footer:cancel()+submit(item?'ذخیرهٔ تغییرات':'ثبت اطلاعات','reference-form')};}
export function deleteReference(kind,x){return {title:'حذف اطلاعات پایه',size:'small',operation:'reference',entityId:x.id,body:`<div class="alert warning">${icon('warning')}«${esc(x.name)}» فقط در صورتی قابل حذف است که در هیچ رکورد یا سابقه‌ای استفاده نشده باشد.</div><form id="delete-reference-form" data-form="delete-reference" data-kind="${kind}" data-id="${x.id}" data-version="${x.version}">${error}${field('علت حذف','reason','',{required:true,textarea:true,max:1000})}</form>`,footer:cancel()+submit('حذف','delete-reference-form','danger','trash')};}
export function passwordDialog(){return {title:'تغییر گذرواژه',subtitle:'پس از تغییر، نشست‌های قبلی شما باطل می‌شوند.',size:'small',operation:'password',body:`<form id="password-form" data-form="password">${error}<div class="form-grid"><div class="span-full">${passwordFields()}</div></div></form>`,footer:cancel()+submit('ذخیرهٔ گذرواژه','password-form')};}
export function resetPassword(u){return {title:'بازنشانی گذرواژهٔ کاربر',subtitle:u.fullName,size:'small',operation:'user.reset',entityId:u.id,body:`<div class="alert warning">${icon('lock')}نشست‌های قبلی کاربر باطل می‌شوند. گذرواژهٔ موقت را امن تحویل دهید؛ کاربر در اولین ورود باید آن را تغییر دهد.</div><form id="reset-password-form" data-form="reset-password" data-id="${u.id}">${error}${field('گذرواژهٔ موقت جدید','newPassword','',{required:true,type:'password',max:128,autocomplete:'new-password',hint:'حداقل ۱۲ نویسه شامل حروف بزرگ و کوچک لاتین، عدد و نماد.'})}${field('تکرار گذرواژهٔ موقت','confirmPassword','',{required:true,type:'password',max:128,autocomplete:'new-password'})}</form>`,footer:cancel()+submit('بازنشانی گذرواژه','reset-password-form','primary','lock')};}
export function importDialog(kind,preview=null){const assets=kind==='assets';return {title:'ورود گروهی '+(assets?'اموال':'کارکنان')+' از Excel',subtitle:'ابتدا اعتبارسنجی، سپس تأیید و ثبت یکپارچه',operation:'import',size:preview?'large':'',body:`<div class="import-steps"><span><b>۱</b>دریافت قالب</span>${icon('left')}<span><b>۲</b>بررسی فایل</span>${icon('left')}<span><b>۳</b>ثبت نهایی</span></div>${!preview?`<div class="alert">${icon('info')}از قالب مخصوص سامانه استفاده کنید و نام ستون‌ها را تغییر ندهید. ${assets?'تصویر و توضیحات برای هر مال الزامی است. راهنمای جاسازی تصویر در کاربرگ «راهنما» قرار دارد.':'کد ملی باید ۱۰ رقم معتبر با حفظ صفر ابتدایی باشد؛ کاربران با نقش عادی ایجاد می‌شوند.'}</div><div class="row-between" style="margin-bottom:17px"><span class="muted" style="font-size:10px">قالب خالی با راهنمای ورود اطلاعات</span>${button('دریافت قالب Excel','download','excel','small','data-url="/api/import/'+kind+'/template"')}</div><form id="import-form" data-form="import-preview" data-kind="${kind}">${error}<div class="import-drop" id="import-drop">${icon('upload')}<h3>فایل تکمیل‌شده را انتخاب کنید</h3><p>فایل XLSX · حداکثر ۲۰ مگابایت و ۱۰۰۰ رکورد</p><input type="file" name="file" accept=".xlsx" required aria-label="انتخاب فایل Excel"></div></form>`:`<div class="import-summary"><div><strong>${fa(preview.total)}</strong><span>کل ردیف‌ها</span></div><div><strong class="text-green">${fa(preview.valid)}</strong><span>ردیف معتبر</span></div><div><strong class="text-red">${fa(preview.errors.length)}</strong><span>ردیف نیازمند اصلاح</span></div></div>${preview.errors.length?`<div class="alert danger">${icon('warning')}هیچ اطلاعاتی ثبت نشده است. خطاهای زیر را در فایل اصلاح و آن را دوباره بررسی کنید.</div><div class="import-errors">${preview.errors.map(e=>`<div class="import-error"><strong>ردیف ${fa(e.row)}</strong><span>${esc(e.message)}</span></div>`).join('')}</div>`:`<div class="alert success">${icon('checkcircle')}همهٔ ردیف‌ها معتبر هستند. با تأیید شما، ${fa(preview.total)} رکورد ثبت خواهد شد. دسته‌بندی یا واحد جدید، در صورت نیاز ساخته می‌شود.</div>`}<h3 style="font-size:12px;margin:17px 0 12px">پیش‌نمایش ${fa(Math.min(10,preview.preview?.length||0))} ردیف نخست</h3><div class="table-scroll"><table class="data-table"><thead><tr><th>ردیف</th><th>کد</th><th>نام</th><th>${assets?'دسته‌بندی':'واحد سازمانی'}</th></tr></thead><tbody>${(preview.preview||[]).map(x=>`<tr><td>${fa(x.row)}</td><td class="ltr">${esc(x.code||'تولید خودکار')}</td><td>${esc(x.name)}</td><td>${esc(x.reference)}</td></tr>`).join('')}</tbody></table></div><form id="import-commit-form" data-form="import-commit" data-kind="${kind}" style="margin-top:17px">${error}${preview.canCommit?'<label class="check-label"><input type="checkbox" required>اطلاعات پیش‌نمایش را بررسی و ثبت نهایی را تأیید می‌کنم.</label>':''}</form>`}`,footer:cancel()+(preview?button('انتخاب فایل دیگر','import-again','upload','','data-kind="'+kind+'"')+(preview.canCommit?submit('ثبت نهایی '+fa(preview.total)+' رکورد','import-commit-form','primary','check'):''):submit('بررسی و پیش‌نمایش','import-form','primary','eye'))};}
const labels={id:'شناسه',code:'کد اموال',oldCode:'کد قبلی',name:'نام',categoryId:'شناسه دسته',brand:'برند',model:'مدل',serial:'سریال',quality:'کیفیت',owner:'مالک',description:'توضیحات',imageId:'شناسه تصویر',purchaseDate:'تاریخ خرید',purchaseCost:'ارزش خرید',hasLabel:'دارای برچسب',status:'وضعیت',location:'محل نگهداری',deleted:'بایگانی‌شده',version:'نسخه رکورد',personnelCode:'کد پرسنلی',firstName:'نام',lastName:'نام خانوادگی',nationalId:'کد ملی ماسک‌شده',username:'نام کاربری',departmentId:'شناسه واحد',role:'نقش',active:'فعال',asset:'اموال',assignment:'تخصیص',assetId:'شناسه اموال',userId:'شناسه شخص',userName:'تحویل‌گیرنده',departmentName:'واحد سازمانی',startedAt:'شروع تخصیص',endedAt:'پایان تخصیص',reference:'شماره مجوز / رسید',notes:'یادداشت',endReason:'علت پایان',effectiveDate:'تاریخ عملیات',amount:'مبلغ فروش',reason:'علت',filename:'نام فایل',size:'اندازه',count:'تعداد',total:'کل',filters:'فیلترها',valid:'معتبر',errors:'خطاها',contentType:'نوع فایل'};
function auditValue(value){if(value===null||value===undefined)return '—';if(typeof value==='boolean')return value?'بله':'خیر';if(typeof value==='number')return fa(value);return statusNames[value]||qualityNames[value]||roleNames[value]||String(value)}
function objHtml(obj,other){if(!obj)return '<div class="muted" style="padding:17px;font-size:11px">مقداری ثبت نشده است.</div>';return Object.entries(obj).map(([k,v])=>typeof v==='object'&&v!==null?`<div class="json-line"><strong class="json-nest">${esc(labels[k]||k)}</strong></div>${objHtml(v,other?.[k])}`:`<div class="json-line ${other&&JSON.stringify(v)!==JSON.stringify(other[k])?'changed':''}"><span>${esc(labels[k]||k)}</span><span>${esc(auditValue(v))}</span></div>`).join('')}
export function auditDetail(x){let before=null,after=null;try{before=JSON.parse(x.before)}catch{}try{after=JSON.parse(x.after)}catch{}return {title:'جزئیات رویداد '+fa(x.id),subtitle:actionNames[x.action]||x.action,size:'large',body:`<div class="alert">${icon('history')}${esc(x.description)}</div><dl class="detail-grid">${detailItem('انجام‌دهنده',x.actorName)}${detailItem('نقش',roleNames[x.actorRole]||'سامانه')}${detailItem('نوع اطلاعات',entityNames[x.entityType]||x.entityType)}${detailItem('تاریخ (تهران)',date(x.at))}${detailItem('زمان دقیق (تهران)',time(x.at))}${detailItem('IP',x.ip)}${detailItem('شناسهٔ پیگیری',x.correlationId,true)}</dl><div class="json-grid"><section class="json-panel"><h3>قبل از تغییر</h3>${objHtml(before,after)}</section><section class="json-panel"><h3>پس از تغییر</h3>${objHtml(after,before)}</section></div><p class="settings-note" style="margin-top:15px">مقادیر تغییرکرده با زمینهٔ زرد مشخص شده‌اند. زمان خام در خروجی ممیزی به UTC ثبت می‌شود.</p>`,footer:button('بستن','close-modal')};}
export function helpDialog(){return {title:'راهنمای امین اموال',subtitle:'مسیر کوتاه شروع کار با سامانه',size:'large',body:`<article class="help-content"><h3>۱. ساختار سازمان را تعریف کنید</h3><p>در «اطلاعات پایه»، دسته‌بندی اموال و واحدهای سازمانی را اضافه کنید. اطلاعات استفاده‌شده در سوابق قابل حذف نیستند.</p><h3>۲. کارکنان را ثبت کنید</h3><p>کد پرسنلی، نام، نام خانوادگی، کد ملی معتبر و واحد را وارد کنید. نام کاربری پیش‌فرض، کد پرسنلی و گذرواژهٔ اولیه مطابق سند، کد ملی است. تغییر رمز در اولین ورود اجباری است. رمز جدید باید حداقل ۱۲ نویسه شامل حروف بزرگ و کوچک لاتین، عدد و نماد باشد.</p><h3>۳. برای هر مال یک شناسنامه بسازید</h3><p>نام، دسته، برند یا مدل، کیفیت، مالک، تصویر و توضیحات اجباری هستند. کد اموال یکتا و در صورت خالی بودن خودکار تولید می‌شود. کد قبلی و سریال در صورت وجود ثبت می‌شوند. ارزش خرید به ریال است.</p><h3>۴. تخصیص، انتقال و عودت</h3><p>شناسنامهٔ مال را باز کنید. اموال موجود در انبار به شخص فعال دارای واحد یا مستقیماً به واحد تخصیص داده می‌شوند. اموال تحویلی امکان انتقال و عودت دارند. تخصیص قبلی در تاریخچه محفوظ است. تاریخ عملیات نباید در آینده یا پیش از رویداد قبلی باشد.</p><h3>۵. فروش، اسقاط و خروج</h3><p>ابتدا اموال را به انبار عودت دهید. از «تغییر وضعیت»، نوع عملیات، تاریخ، علت و شمارهٔ مجوز یا صورت‌جلسه را ثبت کنید. فروش، اسقاط و خروج درخواست مجوز می‌سازند و تنها پس از تأیید یک مدیر غیر از ثبت‌کننده اجرا می‌شوند. درخواست‌ها در «مجوزهای خروج» قابل پیگیری و رد یا لغو هستند. مبلغ فروش الزامی است. فروش، اسقاط و خروج از سازمان نهایی و غیرقابل برگشت هستند. اموال مفقود یا در تعمیر می‌توانند با ثبت مجوز دوباره موجود شوند.</p><h3>۶. ورود و خروج Excel</h3><p>قالب خالی مخصوص هر بخش را دانلود کنید. نام ستون‌ها ثابت است. تاریخ‌ها در فایل، میلادی با قالب YYYY-MM-DD و کد ملی به‌صورت متن هستند. برای تصویر اموال از Insert → Pictures → Place over Cells استفاده کنید؛ گوشهٔ بالا-چپ تصویر باید در ستون «تصویر» و ردیف همان مال باشد. هر فایل حداکثر ۱۰۰۰ ردیف و ۲۰ مگابایت است. قبل از ثبت، پیش‌نمایش نشان داده می‌شود و حتی یک خطا مانع ورود کل فایل است.</p><h3>۷. گزارش‌گیری و ممیزی</h3><p>گزارش جامع، تخصیص جاری و اموال بدون برچسب با فیلترهای ترکیبی و خروجی Excel در دسترس است. خروجی همهٔ ردیف‌های منطبق را تا سقف ۱۰٬۰۰۰ ردیف پوشش می‌دهد. برای تعداد بیشتر، بازه را محدود کنید. مدیر می‌تواند چند مال یا کاربر را انتخاب و سوابق گروهی آنها را ببیند.</p><h3>۸. دسترسی‌ها و حریم خصوصی</h3><ul><li>مدیر: دسترسی کامل، مدیریت نقش‌ها، بایگانی، ممیزی و پشتیبان‌گیری.</li><li>جمعدار: ثبت و ویرایش اموال، کارکنان عادی، تخصیص، عودت، انتقال و گزارش‌ها.</li><li>کاربر عادی: فقط مشاهدهٔ اموال تحویلی خود و تغییر گذرواژهٔ خودش.</li></ul><p>پس از ۵ ورود ناموفق، حساب ۱۵ دقیقه قفل می‌شود. مدیر یا جمعدارِ مجاز می‌تواند رمز موقت جدید تنظیم کند. فایل‌های Excel و پشتیبان حساس‌اند؛ آنها را عمومی به اشتراک نگذارید.</p><h3>۹. پشتیبان‌گیری</h3><p>پشتیبان خودکار هر روز (پیش‌فرض ساعت ۰۲:۰۰ تهران) و نسخهٔ دستی از صفحهٔ تنظیمات تهیه می‌شود. نگهداری پیش‌فرض ۱۴ روز است. نسخه‌ها شامل پایگاه داده، تصاویر، کلیدهای نشست و فهرست SHA-256 هستند؛ نگهداری خارج از سرور بر عهدهٔ بهره‌بردار است. بازیابی فقط آفلاین با ابزار همراه کد انجام می‌شود.</p><h3>نکتهٔ تاریخ‌ها و حساب‌های اولیه</h3><p>تاریخ‌ها در صفحات به تقویم شمسی و زمان تهران نمایش داده می‌شوند. ورودی تقویم استاندارد مرورگر و تاریخ Excel میلادی است. دو حساب راه‌اندازی اولیه اطلاعات ملی ندارند؛ برای تخصیص به آنها ابتدا واحد سازمانی را تعیین کنید.</p></article>`,footer:button('متوجه شدم','close-modal','check','primary')};}
function assetActionButtons(S,a){const id='data-id="'+a.id+'"';if(a.deleted)return `<div class="alert warning">${icon('history')}این مال بایگانی شده؛ شناسنامه و سوابق آن محفوظ است.</div><div class="detail-actions">${button('بازگردانی از بایگانی','restore-asset','refresh','primary',id)}${button('چاپ برچسب','print-label','barcode','',id)}</div>`;if(a.pendingRequestId)return `<div class="alert warning">${icon('shield')}درخواست مجوز در انتظار است؛ ویرایش و تخصیص تا تعیین تکلیف قفل می‌شود.</div><div class="detail-actions"><button class="btn primary" data-nav="operations">پیگیری مجوز</button>${button('چاپ برچسب','print-label','barcode','',id)}</div>`;const terminal=['Sold','Scrapped','Exited'].includes(a.status);return `<div class="detail-actions">${a.status==='Available'?button('تخصیص اموال','assign','assign','primary',id):a.status==='Assigned'?button('انتقال','transfer','transfer','primary',id)+button('عودت به انبار','return','return','',id):''}${button('ویرایش','edit-asset','edit','',id)}${!terminal&&a.status!=='Assigned'?button('تغییر وضعیت','change-status','layers','',id):''}${button('چاپ برچسب','print-label','barcode','',id)}${a.currentAssignment?button('رسید تحویل','print-receipt','download','',`data-id="${a.currentAssignment.id}"`):''}${isAdmin(S)&&a.status!=='Assigned'?button('','delete-asset','trash','icon-only soft-danger',id+' aria-label="بایگانی اموال"'):''}</div>`}
export function restoreAsset(a){return {title:'بازگردانی از بایگانی',subtitle:a.name,size:'small',operation:'asset.restore',entityId:a.id,body:`<div class="alert">کد و تاریخچه حفظ می‌شوند. بازگردانی بایگانی، وضعیت نهایی فروش یا اسقاط را تغییر نمی‌دهد.</div><form id="restore-form" data-form="restore-asset" data-id="${a.id}" data-version="${a.version}">${error}${field('علت بازگردانی','reason','',{required:true,textarea:true,max:1000})}</form>`,footer:cancel()+submit('بازگردانی','restore-form')};}
export function operationDecision(r,decision){const title={approve:'تأیید مجوز و اجرای عملیات',reject:'رد درخواست مجوز',cancel:'لغو درخواست مجوز'}[decision];return {title,subtitle:r.assetName,size:'small',operation:'disposition',entityId:r.assetId,body:`<div class="alert ${decision==='approve'?'warning':''}">${icon('shield')}${esc(r.reference)} · ${statusNames[r.targetStatus]}${r.amount?' · '+fa(r.amount)+' ریال':''}</div><p class="detail-note">${esc(r.reason)}</p><form id="decision-form" data-form="operation-decision" data-id="${r.id}" data-decision="${decision}" data-version="${r.version}">${error}${field('توضیح تصمیم / شمارهٔ تأیید','note','',{required:true,textarea:true,max:1000})}${decision==='approve'?'<label class="check-label"><input type="checkbox" required>مجوز را بررسی کرده‌ام و اجرای نهایی و غیرقابل برگشت این عملیات را تأیید می‌کنم.</label>':''}</form>`,footer:cancel()+submit(decision==='approve'?'تأیید و اجرا':'ثبت تصمیم','decision-form',decision==='approve'?'primary':'soft-danger')};}
